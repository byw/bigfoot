%h2 Sign up
=simple_form_for resource, :url => registration_path(resource_name), :html => {:novalidate => true} do |form|
  = render :partial => 'form', :object => form
=render :partial => "devise/shared/links"
